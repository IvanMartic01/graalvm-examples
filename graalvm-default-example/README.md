# GRAALVM DEFAULT EXAMPLE #

## PREREQUISITES ##

1. Install GraalVM JAVA

1. Run docker, position yourself in docker directory and run `docker compose up - d`
2. Build image, position yourself in root directory and run `./mvnw clean package -Pnative`
3. Position yourself in target directory and run `./app-name`
